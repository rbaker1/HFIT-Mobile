import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

//TimerConfig is the screen that the user will set workout timers
//This is not where the timer will run
export default function TimerConfig() {


  return (
    <View style={styles.container}>
      <Text>I am a placeholder until I have functionality</Text>
    </View>
  );
